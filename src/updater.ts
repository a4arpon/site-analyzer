// Pack self-update hook (analog of `squirrel self update`).
// Intended to fetch the latest rule pack from a registry and swap it in.
// Not yet implemented — kept as the designated home for that feature.
export function appUpdater() {
  return "hello world"
}
